@model IEnumerable<WebApplicationAseguradorasB.Models.tbl_Clientes>

@{
    //ViewBag.Title = "Index";
    ViewBag.Title = "Indice";
}

@*<h2>Index</h2>*@
<h2>Indice</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.clie_Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.clie_Descripcion)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.clie_IdClientePk }) |
            @Html.ActionLink("Details", "Details", new { id=item.clie_IdClientePk }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.clie_IdClientePk })*@
            @Html.ActionLink("Editar", "Edit", new { id = item.clie_IdClientePk }) |
            @Html.ActionLink("Detalles", "Details", new { id = item.clie_IdClientePk }) |
            @Html.ActionLink("Eliminar", "Delete", new { id = item.clie_IdClientePk })
        </td>
    </tr>
}

</table>
