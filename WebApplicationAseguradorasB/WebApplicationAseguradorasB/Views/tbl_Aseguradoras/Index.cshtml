@model IEnumerable<WebApplicationAseguradorasB.Models.tbl_Aseguradoras>

@{
    //ViewBag.Title = "Index";
    ViewBag.Title = "Indice";
}

@*<h2>Index</h2>*@
<h2>Indice</h2>

<p>
    @Html.ActionLink("Crear Nueva", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.aseg_NombreAseguradora)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.aseg_NombreAseguradora)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.aseg_IdAseguradoraPk }) |
            @Html.ActionLink("Details", "Details", new { id=item.aseg_IdAseguradoraPk }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.aseg_IdAseguradoraPk })*@
            @Html.ActionLink("Editar", "Edit", new { id = item.aseg_IdAseguradoraPk }) |
            @Html.ActionLink("Detalles", "Details", new { id = item.aseg_IdAseguradoraPk }) |
            @Html.ActionLink("Eliminar", "Delete", new { id = item.aseg_IdAseguradoraPk })
        </td>
    </tr>
}

</table>
